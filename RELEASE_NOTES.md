# Public Demo Release Notes

## 2026-08-17

- Store browser session state outside the repository and bind it to the current Windows user and computer.
- Restrict recovery runs to identifiers with confirmed missing fields in the pending manifest.
- Preserve valid punctuation when parsing project-name fields from page text.

This public update contains no organization-specific endpoint, credentials, business workbook, vendor data, or production run log.
