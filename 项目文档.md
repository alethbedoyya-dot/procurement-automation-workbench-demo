# 实现与使用说明

## 设计目标

本项目将采购数据处理拆分为可独立执行、可恢复的阶段：Excel 数据准备、授权门户查询、附件下载、附件解析回填、项目追踪表匹配和价格核对。分阶段设计的目的，是让耗时的网页操作与可重复的本地数据处理解耦，减少因单个页面异常而重跑全部流程的需要。

## 流程边界

| 阶段 | 输入 | 输出 | 处理原则 |
| --- | --- | --- | --- |
| 数据准备 | 本地采购工作簿 | 数据表或透视表 | 按当前品类规则生成所需字段。 |
| 门户查询 | 采购凭证号、私有门户配置 | 按采购凭证归档的附件 | 仅在使用者已获授权的系统中运行。 |
| 附件回填 | 已下载附件、待回填清单 | 项目、数量、价格等可确认字段 | 文件缺失、格式不符或解析失败时不写入猜测值。 |
| 项目追踪匹配 | 透视表、项目价格成本节省追踪表 | 价格、计划成本、节省额、核对信息 | 多条候选或不一致结果保留给人工复核。 |
| 价格计算 | 已准备的数据表和价格表 | 老价格、新价格、节省额、核对列 | 仅针对已配置的品类规则执行。 |

## 本地文件与运行产物

| 类型 | 本地位置 | 是否应提交 |
| --- | --- | --- |
| 私有门户配置 | `config.local.json` 或环境变量 | 否 |
| 登录会话 | `auth.json` | 否 |
| 下载附件 | `downloads/<周期>/<品类>/<采购凭证>/` | 否 |
| 待回填清单 | 下载目录下的 JSON 清单 | 否 |
| 业务工作簿、价格表和追踪表 | 使用者的本地受控位置 | 否 |
| 日志、页面诊断和测试产物 | 项目本地目录 | 否 |

公开仓库只保留代码、测试、依赖清单和说明文档。它不包含可用于访问业务系统的默认地址，也不包含真实业务数据。

## 工作台操作顺序

1. 选择采购记录并生成目标数据表或透视表。
2. 添加后续步骤所需的扩展列。
3. 完成企业统一身份认证后，执行采购凭证查询和附件下载。
4. 运行本地附件回填；如仍有缺失项，只补查未完成的采购凭证。
5. 在具备相应本地追踪表时，执行项目匹配和价格核对。

查询阶段不会直接把页面结果写回业务工作簿，而是先保存待回填清单。回填阶段成功后会归档清单；失败项可在修复本地文件或配置后重试。

## 附件分类与人工复核

项目依据授权环境中约定的标准模板附件规则分类下载文件。符合规则的审批邮件会进入解析路径；普通附件、缺少必要字段的邮件、图像型文档或出现多个候选结果的情况不会被强行自动回填。这样可以把自动化与人工判断的边界保持清晰。

## 验证范围

仓库内的 `unittest` 测试使用临时工作簿，验证合成品类配置、`DEMO-MAT-*` 物料筛选和配置模板校验。测试不启动浏览器、不连接门户，也不读取业务文件。真实门户登录、页面选择器、权限状态、附件格式和业务计算结果必须由使用者在有授权的环境中自行验收。

## 维护原则

- 先更新或新增测试，再调整会影响行为的逻辑。
- 不把调试页面、日志、下载附件或业务工作簿加入 Git。
- 私有网址和文件命名规则通过本机配置或受控环境管理，不写入公开说明。
- 如需对外发布，请先重新扫描当前文件与 Git 历史，再确认所有引用均已脱敏。
